fun main() {
    // write your code here
    val fChar = readln().first()
    val sChar = readln().first()
    val tChar = readln().first()
    val endChar = readln().first()

    println(fChar-1)
    println(sChar-1)
    println(tChar-1)
    println(endChar-1)
}
